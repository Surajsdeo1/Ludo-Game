document.addEventListener('DOMContentLoaded', () => {
    const playButton = document.getElementById('Btn_play');

    playButton.addEventListener('click', () => {
        window.location.href = 'plyground.html';
    });
});
